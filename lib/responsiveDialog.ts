/** Scrollable form dialogs (edit/create) on mobile. */
export const responsiveFormDialogClassName =
  "flex max-h-[min(90dvh,720px)] w-full max-w-[calc(100%-1rem)] flex-col gap-4 overflow-y-auto p-4 sm:max-w-lg sm:p-6 md:max-w-xl";

/** Confirm / delete alert dialogs. */
export const responsiveAlertDialogClassName =
  "max-h-[min(90dvh,480px)] overflow-y-auto";
